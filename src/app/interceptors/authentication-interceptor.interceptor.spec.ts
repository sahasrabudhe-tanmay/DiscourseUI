import { AuthenticationInterceptor } from './authentication-interceptor.interceptor';

describe('AuthenticationInterceptor', () => {
  it('should create an instance', () => {
    expect(new AuthenticationInterceptor()).toBeTruthy();
  });
});
